// /transactions/new — Transaction entry form.
// Will provide a form for adding new transactions with account
// selection, category picker, and amount input. Uses Server Actions
// for submission. Implementation tracked in Issue #26.

export default function NewTransactionPage() {
  return (
    <main className="p-6">
      <h1 className="text-2xl font-bold">New Transaction</h1>
      <p className="mt-2 text-zinc-600 dark:text-zinc-400">Coming soon.</p>
    </main>
  );
}
